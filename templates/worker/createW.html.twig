{% extends 'worker/index.html.twig' %}
{% block title %}Creación de trabajador{% endblock %}
{% block content %}
    <div class="col-md-12">
        <div><h1>Crear trabajador</h1></div>
        {{ form_start(workerForm) }}
        {{ form_widget(workerForm) }}
        <button type="submit" class="btn btn-primary">Crear</button>
        {{ form_end(workerForm) }}
    </div>
{% endblock %}